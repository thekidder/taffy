CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

IF(UNIX)
  ADD_CUSTOM_TARGET(svnversion_${CMAKE_PROJECT_NAME} ALL ./svn_update.sh WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}/tools/)
ENDIF(UNIX)
IF(WIN32)
  ADD_CUSTOM_TARGET(svnversion_${CMAKE_PROJECT_NAME} ALL ./svn_update.bat WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}/tools/)
ENDIF(WIN32)
