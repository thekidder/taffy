/*
 * Creates a header using CMake containing the version information for SORE
 */

#ifndef CMAKE_VERSION_H
#define CMAKE_VERSION_H

const char*        SORE_VERSION_NAME  = "SORE";

const unsigned int SORE_VERSION_MAJOR  = ${VERSION_MAJOR};
const unsigned int SORE_VERSION_MINOR  = ${VERSION_MINOR};
const unsigned int SORE_VERSION_PATCH  = ${VERSION_PATCH};

const char*        SORE_VERSION_BRANCH = "${VERSION_BRANCH}";

#endif
